# Copy to .env.local for local testing (never commit real keys).

# Preferred: Cloudflare Worker proxy URL (keeps the API key secret)
# VITE_GOOGLE_STT_URL=https://zizi-google-stt.<your-subdomain>.workers.dev

# Dev-only fallback (restrict key by HTTP referrer to zackyuen.github.io/*)
# VITE_GOOGLE_SPEECH_API_KEY=
